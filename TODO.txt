
1. implement other fs functions and finish filesystem class + according os functions + commands
2. simple file editor (edit command)
3. implement other apis (date, time)
4. create own programs or commands with custom simple programing language
5. graphics library

REALLY NEEDED
- command stack (navigate to previous commands) -> done
- randstring, randfloat -> done
- auto completition !! -> done
- protected files !!! extra command or flags for force deletion -> done (rmdirf, rmf)
- file editor -> done
- editor, syntax highlighting in run files -> done
- file completition in subdir not working from outside the dir: tools/repair.run (no syntax highlighting)
- run python maybe?
- storing binary files!!!! later important if downloading files, images. base64 ? extra function for it? or storing everything as base64?

!! finish programming language, integrate with os
tasks:
- improve wpl, improvment suggestions over code variable declaration
- execute wpl files in extra view like text editor
- graphics library
- done: add modules to autoload, maybe store entry in fs: s for setting: s_autoload: [ m_wpl, m_fs, ...]
- done: text editor: dont ask for save if no modification
- done: partially: update help
- maybe add different help pages like: help modules, or for single commands like man pages on linux
- done: terminate running wpl programs -> works
- programming language is slow, maybe because of all the logs and for every print adding it to the div
- fix slow: ui update now every 32ms in loops, maybe move it to print func, cuz this one needs ui update
- fix: because of all the function calls, logs, and prints in while loop for example, it updates DOM every iteration. maybe check every few iteration for ctrl-c (and update DOM not every time print is called, hard to check, maybe with timing, only update after a specific time)
- inconsistency!!!: maybe add START to ITER
- loop control: add step maybe: ITER 10 TO 1 STEP 2 WITH I START
- done: add break, continue to loop - added and working
- partially done: add lists
- add command line args
add inc, dec command for simple integer manipulation instead of LET X = X+1
- comments added
NEW OS tasks:
integrate with host os, run server that allows executing cmds on real system and send response to webos back
audio player
text editor with mobile support + \n newline in text doesnt work for now
some gui features
done: export files from os, maybe create a command for it (export)
done: call func without assigning to variable (wpl)
- done: use indexed db!! and store files seperately not in oe big json array
- EXEC in wpl bug: executes everything after EXEC like: EXEC touch i, cant put in variable, need fix
- BUG: filenames currently arent allowed to contain special chars,also not spaces:
especially commands fail to work because they use args[1]  !!!!
- encoding issue, ♪ interpreted as latin-1 gets âª
- fixed: BUG: wpl graphics libraryy G* doesnt work if editor opened then called

some tasks outdated / alr done check pls
- for now the language has many features and support but many things need to be put first in variables to be used, e.g. list len, exec, ..., print, ..., this has to do with the handleExpr, maybe move all important methods there, here are some of them:
- because then other functions will work + support for LET will still be kept cuz it also uses expr
- EXEC (maybe both, handle normally and with expr, cuz can also be without output)
- LIST AT
- LENGTH LIST
- FIND
- move all the above handling to handleExpr: done
- partially works: also add lists to expressions, so they work for e.g.: print
maybe move functions to evalExpr, so they can be directly called like follows:
LET RES = ADD 1,2,3,A,B, ...
- maybe also add builtin random functions for games
- many new features and checks were added to expression functions, maybe affects performance!
done:  : expr checker still not good :(
- ctrl-Z not working in editor
done: - allow functions to modify outer variables
done: - function calls cur: CALL func WITH p1,p2 -> func(p1,p2)  much better
-   -> and make some other syntax easier too :)
- file command

- def add at some point:
string functions: SPLIT, JOIN, SUBSTR, toUpper
conversion: str -> lst after reading file, str -> int, ...
math functions: SQRT, POW, ABS
less important:
nested list (maybe important for graphics)
try ... catch



important notes:
the more output there is on the terminal the slower execution of programs take
!!!! iter loop bug with break/cont, maybe cuz of if/else not handled correctly, compare with while loop

Finish Filesystem Layer

Implement remaining FS functions (rename, move, permissions, links, etc.)

Ensure proper error handling (e.g., disk full, file not found)

Directory management (recursive creation, removal)

Support for virtual files (like /dev, /proc, if relevant)


Implement Core OS Functions + Shell Commands

Map FS operations to shell commands (rm, mv, chmod, etc.)

Add command-line feedback (return codes, messages)

Logging system (basic logging or journaling)


Text Editor

Implement basic edit command with line editing or fullscreen UI (like nano)

Include save/cancel functionality, basic navigation

File viewer (cat, more, or less)


Extend System APIs

Date/time API (date, clock, sleep, uptime)

Random number generator

System info APIs (CPU usage, memory stats if tracked)


Custom Programming Language / Scriptable Programs

Basic interpreter (BASIC, stack-based like Forth, or Lisp-like)

Scriptable shell command support

Include functions to access FS, memory, IO


Graphics Library

Framebuffer abstraction (if applicable)

Primitive drawing (lines, rectangles, text)


Windowing system (very basic, if wanted)

User and Permissions System

User login, user switching (login, su)

User data storage (/home/username)

Basic permission model (read/write/execute + ownership)

Groups (if not too complex)


Process Management (Optional but Valuable)

Simulated or real process/task management (multitasking, if applicable)

Job control (background tasks, task switching)

Task listing (ps, kill, etc.)


Memory Management

Basic heap or memory block tracking (if low-level memory ops exist)

Memory protection (optional for higher realism)


Device I/O System

Abstraction for devices (keyboard, screen, storage)

Support for reading/writing to devices via file-like interfaces

Hotplug or detection simulation


Bootstrapping & Configuration

Config file parser (system config, shell profiles)

Environment variables

Startup script support (.profile, init)


Networking Stack (if desired)

Simulated sockets or message passing

Ping, HTTP requests, file transfer


Package System or Modular App Loader

Installable programs

Modular API for third-party apps


Security Layers

User access control

Shell command restrictions

Basic sandboxing (limiting program access)


Documentation / Help System

man command or help output per command

In-OS documentation browser




Best Order:

Finalize Filesystem + FS Commands

Add Users & Permissions

Add APIs (time, random, etc.)

Add Shell Enhancements & File Editor

Add Process Model (if applicable)

Custom Programming Language

Graphics/Windowing or GUI-based tools

Add Device I/O layer

Networking and advanced services

